---
description: EnvNexus 项目开发全局指导原则
alwaysApply: false
globs: []
---
# EnvNexus 项目开发全局指导原则

在参与 EnvNexus 项目的所有开发、设计和代码生成任务时，必须严格遵守以下三大文档边界作为最高准则：

## 1. 产品背景：以《产品白皮书》为准
- **参考文档**：`docs/product-manual.md`
- **作用**：理解“我们在做什么”以及“为什么这么做”。
- **约束**：所有的功能设计和代码实现，必须符合“零信任架构”、“AI 辅助”、“审批式修复”和“全链路审计”的核心产品愿景。不得偏离企业级 ToB / SaaS 的产品定位。

## 2. 功能边界：以《产品需求文档 (PRD)》为准
- **参考文档**：`docs/product-requirements.md`
- **作用**：理解“具体要做哪些功能”以及“业务规则是什么”。
- **约束**：
  - 严格遵守文档中定义的 RBAC 角色权限边界。
  - 严格遵守定义的审批状态机（如 `Pending_User` -> `Approved` 等）。
  - **禁止自我发散**：不得随意臆造 PRD 中未定义的功能（如：不要擅自添加未规划的社交分享功能、不要擅自修改令牌的有效期规则等）。

## 3. 技术实现：以《技术架构文档》为准
- **参考文档**：`docs/technical-architecture.md`
- **作用**：理解“底层是怎么实现的”以及“各模块如何交互”。
- **约束**：
  - 必须遵循已确立的架构拓扑（Platform API, Session Gateway, Job Runner, Agent Core）。
  - 必须采用文档中明确的技术方案（如：零编译 EOF 流式注入、Redis Pub/Sub 跨实例路由、MySQL `FOR UPDATE SKIP LOCKED` 任务调度）。
  - **禁止技术发散**：不得引入与当前架构冲突的新技术栈（如：不要擅自引入 Kafka 替代 Redis Pub/Sub，不要擅自将 MySQL 换成 PostgreSQL）。

---
**AI 助手行为准则**：在回答用户问题或生成代码前，请先在“潜意识”中核对这三份文档的约束。如果用户的要求超出了 PRD 的边界，或者违背了技术架构文档的设计，请主动向用户指出并请求确认，而不是盲目执行。