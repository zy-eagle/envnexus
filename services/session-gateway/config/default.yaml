app:
  name: session-gateway
  env: local
  log_level: info

server:
  host: 0.0.0.0
  port: 8081
  public_ws_url: ws://localhost:8081

redis:
  addr: ${ENX_REDIS_ADDR}
  password: ${ENX_REDIS_PASSWORD}

security:
  session_token_secret: ${ENX_SESSION_TOKEN_SECRET}
